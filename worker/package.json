{
  "private": true,
  "name": "cloudflare-ha-worker",
  "version": "1.0.0",
  "description": "A reverse proxy and queuing agent for Home Assistant",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "Ian Tucker",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}